export default function Settings() {
  return (
    <div>
      <h1>Settings</h1>
      {/* TODO: Account section — name, email, avatar, sign out */}
      {/* TODO: Appearance — dark/light mode, language (EN/RO) */}
      {/* TODO: Notifications — enable/disable, time picker */}
      {/* TODO: Calendar — week start day */}
      {/* TODO: General — default landing page */}
      {/* TODO: Spotify — connect/disconnect */}
    </div>
  )
}
